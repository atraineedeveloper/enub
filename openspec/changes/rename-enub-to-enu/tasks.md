# Tasks

- [ ] Inventory current `ENUB`, `Enub` and relevant lowercase `enub` occurrences.
- [ ] Classify each occurrence as user-facing brand, current documentation, technical identifier or history.
- [ ] Rename the authenticated header and other visible application copy to `ENU`.
- [ ] Rename browser and PWA metadata to `ENU` without renaming existing asset files.
- [ ] Rename branding in current PDF exporters.
- [ ] Rename branding in active Supabase authentication email templates and local seed copy that users can receive or see.
- [ ] Update current README and active developer documentation where the product name is descriptive.
- [ ] Update focused tests whose expected visible copy changes.
- [ ] Run a final case-sensitive search and document intentional exclusions.
- [ ] Run `bun run typecheck`, `bun run lint`, relevant tests and `bun run build`.
- [ ] Manually verify the authenticated header, PWA metadata, one authentication email and one representative PDF.