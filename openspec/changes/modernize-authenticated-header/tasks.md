## 1. Worker-identity data source (missing-row vs. query-failure)

- [x] 1.1 Add `getWorkerIdentityById(id)` to `src/services/apiWorkers.ts` using `.select("name, profile_picture").eq("id", id).maybeSingle()`, returning `null` when no row matches and throwing only on a genuine error. Leave `getWorkerById()` (`.single()`) unchanged for its existing callers.
- [x] 1.2 Write a `bun:test` unit test (with the Supabase client call mocked/stubbed) covering: row found, no row found (`null`, no throw), and a simulated error (throws).

## 2. Worker-query gating

- [x] 2.1 Implement `canFetchWorkerIdentity({ profileSucceeded, role, workerId })` as a pure, exported function (e.g. in `useCurrentIdentity.ts` or a small co-located module).
- [x] 2.2 Relax `useWorker`/introduce a worker-identity query in `src/features/authentication/useCurrentIdentity.ts` whose `enabled` option is exactly `canFetchWorkerIdentity(...)`, not `workerId != null`. (Implemented as a dedicated `["worker-identity", authUserId, workerId]` query using `getWorkerIdentityById`, not a reuse of `useWorker`. Superseded/corrected in §13 below: the query key now also binds to `authUserId`, and gating requires a valid `authUserId` too.)
- [x] 2.3 Write `bun:test` unit tests for `canFetchWorkerIdentity`: admin → false; staff → false; worker with missing id → false; worker with `0`, negative, `NaN`, or non-integer id → false; worker with a valid positive integer id → true.

## 3. Identity-state model

- [x] 3.1 Create `src/features/authentication/useCurrentIdentity.ts` returning the discriminated `IdentityState` union (`loading | ready | incomplete | denied | profile-error | worker-error`) per design.md §3, composing `useProfile()` and the gated worker-identity query from Task Group 2. A `worker`-role profile whose `workerId` fails `canFetchWorkerIdentity` gating resolves to `incomplete` (never `ready` with a fallback identity) — no worker lookup is attempted for it.
- [x] 3.2 Implement the exact role-label mapping (`admin` → "Administrador", `staff` → "Personal administrativo", `worker` → "Docente"; anything else → `denied`, no label).
- [x] 3.3 Implement display-name resolution: worker row found → `workers.name`; *valid* `workerId` with worker row missing → email-local-part fallback (`ready`); `admin`/`staff` → email-local-part fallback (`ready`); invalid/absent `workerId` → no identity resolution at all (`incomplete`, no fallback name computed or shown). (Also covers a worker row found with a blank/whitespace-only name, which falls back the same way.)
- [x] 3.4 Ensure `useCurrentIdentity()` derives its return value fresh from `useProfile()`/query state on every render — no `useState`/`useRef` retention of derived identity fields (design.md §5).
- [x] 3.5 Implement the current-user/profile-generation guard as a pure function (`resolveIdentityState(profile: ProfileSnapshot, worker: WorkerQuerySnapshot | null)` per design.md §5) that rejects a worker-query snapshot whenever its `forAuthUserId`/`forWorkerId` don't match the current profile's `authUserId`/`workerId`, treating a rejected snapshot as if no worker result existed yet. (Corrected in §13 below: the authenticated user id is now *also* included in the worker query's cache key, as defense-in-depth alongside this resolver-level guard, not instead of it — see §13.1.)
- [x] 3.6 Write `bun:test` unit tests for the full state-resolution table in design.md §3, including: loading; profile-error; incomplete (no profile row); incomplete (worker role, missing workerId); incomplete (worker role, `workerId: 0`); incomplete (worker role, negative workerId); incomplete (worker role, non-integer workerId); incomplete (worker role, `NaN`/`Infinity` workerId); denied; ready-admin; ready-staff; ready-worker-found; ready-worker-valid-id-missing-row; worker-error (valid id, query fails).
- [x] 3.7 Write `bun:test` resolver/state-snapshot tests (not full React Query integration tests) for `resolveIdentityState` covering: current user changes while cached worker data exists (rejected); the same `workerId` reused across two different user snapshots (not trusted until regenerated for the new user); an old worker result supplied alongside a still-unresolved (loading) profile (resolver yields `loading`, ignoring the stale snapshot); a failed logout leaves the current identity's snapshot and resolved state unchanged; a successful account change yields a neutral/loading state until the new profile resolves.
- [x] 3.8 (Discovered during implementation, not separately listed above) Widen `useProfile()`'s return shape to also expose `isError` from its underlying query, since the existing hook previously had no way to distinguish "profile query failed" from "no profiles row" — both collapsed to `role: null`. Required for `profile-error` to be detectable at all.

## 4. Avatar component

- [x] 4.1 Create `src/ui/Avatar.tsx`: `<Avatar src? name size />`, generalizing the `Img`/`Avatar` styled-elements pattern from `WorkerRow.tsx`.
- [x] 4.2 Implement the fallback order: valid `src` (from `getProfilePicturePublicUrl`, passed in by the caller — never derived from name/email) → initials → generic accessible icon.
- [x] 4.3 Add `onError` handling on the `<img>` so a failed image swaps to the initials fallback immediately, never leaving a broken-image icon.
- [x] 4.4 Ensure the image-error flag resets when the `src` prop changes (e.g. via `key={src}` or an effect keyed on `src`), so a stale failure doesn't block a later valid image. (Implemented via a `useEffect` keyed on `src`.)
- [x] 4.5 Implement a Unicode-safe, exported `getInitials(name: string)` helper: `\p{L}`-aware, one-word → one initial, multi-word → first+last meaningful letters, empty/malformed input → `""` (component falls back to the generic icon). (In `src/ui/avatarIdentity.ts`.)
- [x] 4.6 Implement an exported `resolveAvatarDisplay({ src, hasImageError, name })` pure decision helper (image vs. initials vs. icon) for direct unit testing. (In `src/ui/avatarIdentity.ts`.)
- [x] 4.7 Use existing `--color-brand-*` tokens for the initials background/text, matching `WorkerRow.tsx`'s convention.
- [x] 4.8 Mark the avatar `aria-hidden="true"` when used inside the account trigger button (which carries its own accessible name); support a standalone `name`/`alt` for any other usage. (`decorative` prop.)
- [x] 4.9 Write `bun:test` unit tests for `getInitials` (accented Unicode letters, one-word, multi-word, empty/malformed) and `resolveAvatarDisplay` (all fallback branches, including "image errored"). (`src/ui/avatarIdentity.test.ts`.)
- [x] 4.10 Implement the email-local-part extraction helper (`nameFromEmail`) that strips the domain and normalizes separators to spaces *before* the result is ever passed to `getInitials`; write a `bun:test` test asserting the domain never appears in computed initials. (`src/ui/avatarIdentity.ts` / `.test.ts`.)

## 5. Route-context resolver

- [x] 5.1 Create `src/ui/routeContext.ts` with the ordered, most-specific-first pattern list from design.md's Decision 5, covering at least: `/workers/:id/documents` → "Documentos del trabajador", `/workers/:id` → "Detalle del trabajador", `/workers` → "Trabajadores", `/semesters/:id` → "Horario del semestre", `/semesters` → "Semestres", `/dashboard` → "Inicio", `/my-documents` → "Mis documentos", and the remaining routes from `src/App.tsx`.
- [x] 5.2 Implement resolution using `react-router-dom`'s `matchPath` against the ordered list, returning the first match's label or the `"ENUB"` fallback when nothing matches.
- [x] 5.3 Wire the resolver into `Header` via `useLocation()`, confirming the label updates on navigation without a full header remount.
- [x] 5.4 Write `bun:test` unit tests for: most-specific-wins ordering (`/workers/42/documents` vs. `/workers/42` vs. `/workers`), the exact semester-detail label test (`/semesters/7` → exactly "Horario del semestre", distinct from `/semesters` → exactly "Semestres", confirming the specific pattern wins), each other explicitly required mapping, and the unknown-route fallback.

## 6. Account popover

- [x] 6.1 Create `src/ui/AccountPopover.tsx` with a trigger `button` (`aria-expanded`, `aria-controls`, `aria-label="Abrir opciones de cuenta de {name}"`) and a popover region (`role="region"`/labeled group — not `role="menu"`) containing display name, role label, and a `Cerrar sesión` button.
- [x] 6.2 Wire outside-click dismissal via the existing `useOutsideClick` hook. On outside click, close the popover only — do not forcibly move focus anywhere.
- [x] 6.3 Add `Escape`-to-close keyboard handling, scoped to the open popover. Only this keyboard-dismissal path returns focus to the trigger button.
- [x] 6.4 Ensure ordinary `Tab`/`Shift+Tab` order through the popover's real interactive elements — no roving `tabindex`, no arrow-key handling. (No custom tabindex/keydown-arrow logic added; native tab order applies.)
- [x] 6.5 Close the popover on route navigation (reset open state on `pathname` change). Do not return focus to the old trigger on this path.
- [x] 6.6 Wire the popover's `Cerrar sesión` button to the existing `useLogout()` hook; disable while `isLoading`; prevent duplicate submission. Do not layer any additional focus-restoration logic onto the logout flow's own navigation.
- [x] 6.7 Ensure the popover and its trigger are only rendered when identity state is `ready`. (Enforced by the caller — `AccountPopover` is only mounted by `Header` when `identity.status === "ready"`; see Header rewrite.)

## 7. Minimal-header direct logout control

- [x] 7.1 Implement a direct logout `button` (distinct markup from the popover trigger) shown whenever identity state is `loading`, `incomplete`, `denied`, `profile-error`, or `worker-error`. (`src/ui/DirectLogoutButton.tsx`.)
- [x] 7.2 Wire it to the same `useLogout()` hook used by the popover — no second logout implementation; disable while `isLoading`; prevent duplicate submission.
- [x] 7.3 Confirm keyboard accessibility (real `button`, focusable, activatable via Enter/Space). (Native `<button>`, no custom handling needed.)

## 8. Header composition and worker-layout alignment

- [x] 8.1 Rewrite `src/ui/Header.tsx` into the three-region flex layout (left: toggle?/brand/subtitle; context: route label; right: theme toggle + account control), removing the empty `<span />` placeholder used today for fake centering.
- [x] 8.2 Keep the `onToggleSidebar?: () => void` prop contract unchanged so `AppLayout.tsx` and `WorkerAppLayout.tsx` require no call-site changes.
- [x] 8.3 Render `AccountPopover` when identity state is `ready`; render the direct logout control (Task Group 7) plus neutral placeholder logic for `loading` in all other states.
- [x] 8.4 Keep the `<header>` element as the semantic landmark; keep all interactive controls as real `button` elements.
- [x] 8.5 Verify visually that the worker layout (no toggle) and the admin layout (toggle present) both align correctly with no leftover placeholder gap. (Verified via Playwright against a local Supabase instance with seeded admin/worker accounts — both headers left-align cleanly with no gap.)

## 9. Responsive and touch-target behavior

- [x] 9.1 Implement responsive collapsing using exactly two fixed `max-width` media-query breakpoints — no container queries: the existing `900px` mobile breakpoint, and one new, documented intermediate threshold at `1100px`.
- [x] 9.2 At the intermediate tier (`901px`–`1100px`): hide the subtitle (`display: none`); let the route-context label truncate with `flex-shrink: 1`/`text-overflow: ellipsis` rather than disappearing outright; keep name/role/avatar fully visible.
- [x] 9.3 At the mobile tier (`≤ 900px`): fully hide the route-context label and the visible name/role text (`display: none`); show only the menu toggle (if present), compact brand, and the avatar/account trigger (or direct controls in non-ready states); name/role/logout remain available inside the popover.
- [x] 9.4 Set a fixed `max-width` + ellipsis (`overflow: hidden; text-overflow: ellipsis; white-space: nowrap`) on the name span, with the full name available via `title` and inside the popover.
- [x] 9.5 Apply `min-width: 0` on flexible header regions to guarantee no horizontal overflow.
- [x] 9.6 Widen all interactive header controls (sidebar toggle, theme toggle, account/avatar trigger, direct logout, popover buttons) to a minimum 44×44 CSS px hit area via padding/`min-width`/`min-height`, without enlarging icon glyphs.
- [x] 9.7 Manually verify no horizontal overflow or control overlap at desktop (`>1100px`), intermediate (`901px`–`1100px`), and mobile (`≤900px`) widths, using the seeded local accounts' real (short) names. (Verified via Playwright at 1280px, 1000px, and 390px — no overflow, correct collapse at each tier.)
- [ ] 9.8 Manually verify a synthetic very-long display name at each breakpoint (no overflow, ellipsis truncation, full name in `title` and inside the popover). Not yet performed live — the seeded local accounts all have short names, so this specific scenario has not been exercised in a real browser.

## 10. Accessibility pass

- [x] 10.1 Add a missing `aria-label` to the theme toggle (`DarkModeToggle.tsx`) — currently absent.
- [x] 10.2 Confirm the theme toggle and both logout controls (popover + direct) retain accessible names after being moved/restyled.
- [x] 10.3 Manually verify keyboard-only operation: sidebar/menu toggle → theme toggle → account trigger → open popover → Tab through items → Escape → focus returns to trigger. Separately verify outside-click close does not forcibly move focus, and route-change close does not return focus to the (now-gone) old trigger. (Verified via Playwright: clean Tab traversal from a blurred body confirms the exact order "Abrir menú" → "Activar modo oscuro" → "Abrir opciones de cuenta de {name}" → sidebar links; Enter opens the popover; Tab moves into it and reaches "Cerrar sesión"; Escape closes it and returns focus to the trigger; outside click and route navigation both close it without any focus assertion — matching the no-forced-focus-move / no-return-to-old-trigger requirements.)
- [x] 10.4 Manually verify no header interaction depends solely on `:hover` (all actions reachable via focus + click/keyboard activation). (Confirmed by construction and by the keyboard-only pass above — every interactive header/popover element is a real `button` with `onClick`, no `:hover`-only handlers exist anywhere in the new code.)

## 11. Dark mode verification

- [x] 11.1 Verify header surface, popover background/border/shadow, primary text, secondary text (role/subtitle/context), focus ring, avatar fallback colors, and hover/focus states in both light and dark mode, using only existing CSS custom properties (design.md §12). (Verified via Playwright/screenshots: header surface, avatar initials, role/context text, and the opened account popover all render correctly against the dark tokens with no unstyled/invisible elements; focus ring uses the existing global `button:focus` convention, unaffected by dark mode.)
- [ ] 11.2 Manually verify the logout pending/disabled/spinner visual state in both light and dark mode, from both the popover and the direct control. Not yet performed live — logout completed too quickly in the automated pass to observe the pending state; the code path (`disabled={isLoading}` + conditional `SpinnerMini`) is confirmed by source inspection only (Task Group 7 / §5 below), not by observing it render.

## 12. Cleanup and verification

- [x] 12.1 Confirm `src/features/authentication/Logout.tsx`'s existing hook/mutation logic is reused by both the popover and direct logout controls (Task Groups 6-7), removing any now-redundant standalone icon-only rendering path if fully superseded. (`Logout.tsx` deleted — it was only imported by `Header.tsx`; both `AccountPopover` and `DirectLogoutButton` call `useLogout()` directly.)
- [x] 12.2 Run `bun run lint`. (Clean, no errors.)
- [x] 12.3 Run `bun run typecheck`. (Clean, no errors.)
- [x] 12.4 Run `bun run build`. (Succeeds.)
- [x] 12.5 Run `bun test` for the new unit tests (Task Groups 1-5). (`bun test src/` — 96 pass, 0 fail, across 6 files. The two pre-existing `Deno.test` Edge Function suites under `supabase/functions/` are unrelated to this change and require `deno test`, not `bun test`.)
- [x] 12.6 Manually verify the browser matrix items actually exercised live (Playwright, chromium, against a local Supabase instance started via `bunx supabase start` with the repo's seeded local accounts — `admin.local@enub.test`, `trabajador.vinculado.local@enub.test`, `auth-sin-perfil.local@enub.test`): admin desktop, admin dark mode, admin mobile, teacher (worker) desktop, teacher mobile, keyboard-only navigation (menu toggle → theme toggle → account trigger → Enter opens popover → Tab reaches "Cerrar sesión" → Escape closes and returns focus), outside-click close (no forced focus move), route-change close (no focus return to old trigger), missing profile image (both seeded accounts fall back to initials, since neither has a stored `profile_picture`), pending/no-access minimal header (the `auth-sin-perfil` account — no profile row → `incomplete` → brand + theme toggle + direct logout only, no trigger/name/role), successful logout (navigates to `/login`), worker layout alignment without sidebar toggle, and the intermediate tier (`901px`–`1100px`) subtitle/route-context collapse.
- [ ] 12.6a Manually verify a `staff`-role account's header (role label "Personal administrativo", distinct from "Administrador"). Not yet performed live — no `staff`-role account exists in the repo's local seed data (only `admin`/`worker` are seeded); would require creating one (e.g. via local Supabase Studio/SQL) before this can be exercised in a real browser.
- [ ] 12.6b Manually verify a deliberately broken profile image URL falls back to initials with no broken-image icon shown. Not yet performed live — unit-tested only (`avatarIdentity.test.ts`'s `resolveAvatarDisplay` "errored image" cases).
- [ ] 12.6c Manually verify that replacing a broken image's `src` with a new valid source recovers (the error flag resets and the new image attempts to load) in a real browser. Not yet performed live — unit-tested only (`Avatar.tsx`'s `useEffect` keyed on `src`, exercised indirectly by the pure `resolveAvatarDisplay` tests, not by a real `<img>` error/reload cycle).
- [ ] 12.6d Manually verify a worker profile with an invalid or missing `workerId` renders the minimal header (`incomplete`, no fallback identity). Not yet performed live — the `auth-sin-perfil` account exercised the "no profile row" `incomplete` path (12.6, above), which renders the same minimal header, but not the "worker role with an invalid `workerId`" trigger specifically; unit-tested only (`useCurrentIdentity.test.ts`).
- [ ] 12.6e Manually verify live navigation to `/semesters/:id` shows exactly "Horario del semestre", distinct from `/semesters`'s "Semestres". Not yet performed live — unit-tested only (`routeContext.test.ts`).
- [x] 12.7 Confirm Supabase env vars remain correctly documented (no changes expected, per `AGENTS.md`). (Confirmed — no new env vars: `getWorkerIdentityById` uses the same `supabase` client and the same `workers` table/RLS already relied on by `getWorkerById`.)
- [x] 12.8 Update any relevant project documentation if header behavior, component structure, or shared UI primitives changed in a way future contributors need to know about. (Checked `README.md`/`AGENTS.md` for `Header`/`Logout` references — none exist; no external documentation describes the header's internals, so none needed updating.)

## 13. Post-implementation corrections (final findings round)

- [x] 13.1 Bind the worker-identity query key to the authenticated user: changed `["worker-identity", workerId]` to `["worker-identity", authUserId, workerId]` in `useCurrentIdentity.ts`, so two different authenticated users who resolve the same `workerId` get distinct cache entries — React Query itself can no longer hand back one user's cached result for another user's key.
- [x] 13.2 Extend `canFetchWorkerIdentity`'s gate to also require `authUserId` to be a non-empty string (missing, empty-string, and whitespace-only `authUserId` all fail the gate), not just a valid `workerId` and `role === "worker"`.
- [x] 13.3 Stop stamping the current render's `authUserId`/`workerId` onto the worker-query result after the fact. Added `fetchWorkerIdentitySnapshot(authUserId, workerId, fetchWorkerRow?)`, whose closure captures `authUserId`/`workerId` at the moment the request is built and returns them *as part of its resolved payload* (`{ forAuthUserId, forWorkerId, data }`). The hook now reads `forAuthUserId`/`forWorkerId` from `workerQuery.data` (the captured payload) when constructing the snapshot passed to the resolver, rather than reading the current render's live `authUserId`/`workerId` variables. Added `buildWorkerIdentityQueryOptions(authUserId, workerId)` so the exact `{ queryKey, queryFn }` shape handed to `useQuery` is itself a testable unit.
- [x] 13.4 Add runtime-representative tests (`useCurrentIdentity.test.ts`, new `describe` block, explicitly labeled as not a QueryClient integration test) proving: user A + worker 5 and user B + worker 5 produce distinct `queryKey`s; a plain key-based cache populated under user A's key cannot satisfy a lookup under user B's key; `fetchWorkerIdentitySnapshot` preserves the `authUserId` captured when the request started even if an "ambient current user" variable changes before the promise resolves (a real `await` race, not a mocked one); and an end-to-end run through `buildWorkerIdentityQueryOptions` → `fetchWorkerIdentitySnapshot` → `resolveIdentityState` for two different generations produces distinct query keys and a `ready` result built only from the requesting generation's own data. `getWorkerIdentityById` is not mocked or hit over the network in these tests — `fetchWorkerIdentitySnapshot` takes an injectable `fetchWorkerRow` parameter (defaulting to the real service call in production) so the tests can supply a fake fetcher directly, with no module mocking and no risk of cross-test-file interference.
- [x] 13.5 Re-confirm the pure generation guard (`resolveIdentityState`) still rejects mismatched snapshots after the query-key change — existing tests in the "current-user/profile-generation guard" `describe` block pass unchanged (the resolver-level guard is intentionally kept as defense-in-depth alongside the new query-key binding, not replaced by it).
- [x] 13.6 Add the missing-authUserId gating cases to `canFetchWorkerIdentity`'s test suite: missing (`null`), empty-string, and whitespace-only `authUserId` all fail the gate even with an otherwise-valid `workerId`; added a matching `resolveIdentityState` case (missing `authUserId` with an otherwise-valid `workerId` resolves to `incomplete`).
- [x] 13.7 Add the `"Usuario"` fallback: when no worker name and no usable email local-part exist (missing email, empty email, malformed email with an empty local part, or a whitespace-only email), `enrichResolution` now falls back to the exported constant `FALLBACK_DISPLAY_NAME = "Usuario"` instead of an empty string — applied uniformly to `admin`, `staff`, and `worker` fallback resolution (all three flow through the same code path). `getInitials("Usuario")` naturally yields `"U"`. Since `AccountPopover`'s trigger label is `` `Abrir opciones de cuenta de ${identity.displayName}` ``, this also fixes the accessible name, which now reads exactly `"Abrir opciones de cuenta de Usuario"` and is never blank or trailing-space-only.
- [x] 13.8 Add tests for the `"Usuario"` fallback: missing email, empty email, malformed email with an empty local part, whitespace-only email, and a worker row with a blank name *and* no usable email (compounding both fallback triggers) — all resolve to `displayName: "Usuario"` / `initials: "U"`; a dedicated test also asserts the resulting accessible trigger label string is exactly `"Abrir opciones de cuenta de Usuario"`, never blank or trailing-space-only.
- [x] 13.9 Remove the stale `Logout.tsx` reference in `useLogout.ts`'s comment (it named `Logout.tsx` as "its one caller"; `Logout.tsx` was deleted in Task Group 12 of this same change). Updated to name the actual current callers, `AccountPopover.tsx` and `DirectLogoutButton.tsx`, with a one-line historical note that the original caller was removed. Confirmed via `grep` that no import of, or runtime reference to, the removed `Logout.tsx` remains anywhere in `src/`.
- [x] 13.10 Re-split the manual-verification ledger (Task Groups 9, 11, 12) so items not actually exercised live are left unchecked rather than folded into a single checked task with a caveat: added 9.8 (long display name), 11.2 (logout pending/disabled/spinner visual), and 12.6a–12.6e (staff account, broken image, image-source-replacement-after-error, invalid/missing-`workerId` minimal header live trigger, live `/semesters/:id` navigation) as their own unchecked items. 12.6 itself now claims only the browser matrix items that were genuinely exercised live.
- [x] 13.11 Confirm in code (not by claiming browser verification) that both logout controls prevent duplicate activation while pending: `AccountPopover.tsx`'s `LogoutButton` and `DirectLogoutButton.tsx`'s `StyledButton` both pass `disabled={isLoading}` (aliased `isLoggingOut` in the popover) from the same `useLogout()` hook, both render a `SpinnerMini` in place of their icon while `isLoading` is true, and both are native `<button disabled>` elements, which the browser itself prevents from dispatching further `click`/`onClick` activations while disabled — no separate debounce/guard logic was needed or added.
